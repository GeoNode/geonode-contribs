from django.apps import AppConfig


class KeycloaksyncConfig(AppConfig):
    name = 'keycloaksync'
    label = 'keycloaksync'
    verbose_name = 'Keycloak Sync'  # Sets the display name within django administration.
